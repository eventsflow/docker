# ownport/eventsflow-base image

Docker image for python3 based on Ubuntu Linux

## How to use

```
$ docker pull ownport/eventsflow-base:<version>-ubuntu
```
where `version` is the current image version

